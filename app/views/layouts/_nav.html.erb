<nav class="navbar navbar-default navbar-absolute">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand navbar-uppercasedata: { confirm: "Are you sure?" }">
        <%= content_for?(:title) ? yield(:title) : '' %>
        <!--Loading in the titles defined on each page -->
      </a>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <!-- Using rails helpers  -->
        <li>
          <%= link_to '<i class="material-icons">home</i>
          <p class="hidden-lg hidden-md">Home</p>'.html_safe, root_path, class: 'dropdown-toggle' %>
        </li>

        <li>
          <%= link_to '<i class="material-icons">settings</i>
          <p class="hidden-lg hidden-md">Settings</p>'.html_safe, edit_user_registration_path(current_user), class: 'dropdown-toggle' %>
        </li>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <i class="material-icons">person</i><%= current_user.first_name + ' ' + current_user.last_name %><i class="material-icons">keyboard_arrow_down</i>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to '<i class="fa fa-user" aria-hidden="true"></i> &nbspMy Profile'.html_safe, user_path(current_user), class: 'dropdown-toggle' %></li>
            <li><%= link_to('<i class="fa fa-sign-out" aria-hidden="true"></i> &nbspSign Out'.html_safe, destroy_user_session_path, data: { confirm: "Are you sure you want to sign out?" }, :method => :delete) %></li>
          </ul>

        </li>

      </ul>
 <!-- Putting a form inside the nav bar. This can be used to search for all users, gyms etc
 -->
      <%= form_tag users_path, :method => 'get', class: 'navbar-form navbar-right', role: 'search' do %>
        <div class="form-group is-empty">
          <%= text_field_tag :search, params[:search], placeholder: 'Search', class: 'form-control' %>
          <span class="material-input"></span>
        </div>
        <%= button_tag '<i class="material-icons">search</i><div class="ripple-container"></div>'.html_safe, class: 'btn btn-white btn-round btn-just-icon' %>
      <% end %>
    </div>
  </div>
</nav>
